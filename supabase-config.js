// ============================================================
// Supabase project credentials — Portal do Ensino InCor
// ============================================================
//
// How to fill in the values below:
//   1. Go to https://supabase.com/dashboard and open your project.
//   2. Navigate to Project Settings → API.
//   3. Copy:
//        • "Project URL"          → paste as SUPABASE_URL
//        • "anon / public" key    → paste as SUPABASE_ANON_KEY
//
// ⚠️  IMPORTANT — Security rules:
//   • SUPABASE_ANON_KEY is the *publishable* key; it is safe to include
//     in browser code, but keep Row Level Security (RLS) enabled.
//   • NEVER put the service_role / secret key (sb_secret_*) in this file.
//   • If you accidentally commit a real key, rotate it immediately in
//     Supabase Dashboard → Settings → API Keys.
//
// For local development you can replace the placeholders here directly.
// For production (GitHub Pages, Vercel, Netlify, etc.) prefer injecting
// the values through your hosting provider's environment / secrets panel
// and replacing the placeholders at build time.
// ============================================================

export const SUPABASE_URL = "YOUR_SUPABASE_URL";
export const SUPABASE_ANON_KEY = "YOUR_SUPABASE_ANON_KEY";
